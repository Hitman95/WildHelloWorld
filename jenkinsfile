pipeline {
    agent {
        CentOS-Docker
    }
    stages {
        docker {
            image 'maven:latest' 
            args '-v /root/.m2:/root/.m2' 
        }
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}